# Normaliza fim de linha: o repositório guarda LF e cada máquina usa o padrão dela.
# Sem isto, abrir os arquivos no Windows (CRLF) fazia todos aparecerem como modificados.
* text=auto

*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
*.woff2 binary
